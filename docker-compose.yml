services:
  web:
    build: .
    command: bin/rails server -b 0.0.0.0 -p 3001
    environment:
      RAILS_ENV: development
      RAILS_LOG_TO_STDOUT: "true"
    ports:
      - "3001:3001"
    volumes:
      - .:/rails
